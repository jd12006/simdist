simdist
--------

A Python package to simulate numbers from a distribution and save them as a numpy array. 

To use (with caution), simply do::

    >>> import simdist
    >>> simdist.simulate_distribution(50, "normal")